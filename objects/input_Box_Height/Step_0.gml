/// @description Insert description here
// You can write your code in this editor



 if collision_point(mouse_x, mouse_y, input_Box_Height, true, false)
 {
	 
if (string_length(keyboard_string)<limit) 
    height = keyboard_string;
else
    keyboard_string = height;
 }
/*	
if keyboard_check(vk_enter){

weight = string_digits(message);
if(weight > 0)
{
	global.weight = weight;
	room_goto(Room1);
}
show_debug_message("\nis digit ");
show_debug_message(weight);

}
*/

